const DB_KEYS=['proqtrack_db_v6','proqtrack_db_v7'];
function read(){try{return JSON.parse(localStorage.getItem(DB_KEYS[0])||localStorage.getItem(DB_KEYS[1])||'{}')||{};}catch{return{};}}
function write(db){const text=JSON.stringify(db);DB_KEYS.forEach(k=>localStorage.setItem(k,text));window.dispatchEvent(new CustomEvent('proqtrack:db-updated',{detail:{reason:'client-logo-auto'}}));}
function normalizeWebsite(value){let raw=String(value||'').trim();if(!raw)return'';if(!/^https?:\/\//i.test(raw))raw=`https://${raw}`;try{const url=new URL(raw);return['http:','https:'].includes(url.protocol)?url.origin:'';}catch{return'';}}
function candidateUrls(website){const origin=normalizeWebsite(website);if(!origin)return[];const host=new URL(origin).hostname;return[`${origin}/apple-touch-icon.png`,`${origin}/apple-touch-icon-precomposed.png`,`${origin}/favicon-192x192.png`,`${origin}/favicon-96x96.png`,`${origin}/favicon-32x32.png`,`${origin}/favicon.ico`,`https://www.google.com/s2/favicons?domain=${encodeURIComponent(host)}&sz=256`];}
function canLoadImage(url,timeout=5000){return new Promise(resolve=>{const img=new Image();let done=false;const finish=value=>{if(done)return;done=true;clearTimeout(timer);img.onload=null;img.onerror=null;resolve(value);};const timer=setTimeout(()=>finish(false),timeout);img.onload=()=>finish(Boolean(img.naturalWidth&&img.naturalHeight));img.onerror=()=>finish(false);img.referrerPolicy='no-referrer';img.src=url;});}
async function resolveLogo(website){for(const url of candidateUrls(website)){if(await canLoadImage(url))return{url,type:url.includes('google.com/s2/favicons')?'favicon_service':url.endsWith('.ico')?'favicon':'site_icon'};}return null;}
function findWebsiteField(form){return form.querySelector('input[name="website"],input[name="websiteUrl"],input[name="url"],input[type="url"]');}
function findNameField(form){return form.querySelector('input[name="name"],input[name="clientName"],input[name="legalName"]');}
function looksLikeClientForm(form){if(!findWebsiteField(form))return false;const text=`${form.id} ${form.className} ${form.textContent}`.toLowerCase();return /client|klien/.test(text)||Boolean(form.querySelector('input[name="picName"],input[name="legalName"],input[name="industry"]'));}
async function updateSavedClient({website,name,previousWebsite}){const normalized=normalizeWebsite(website);if(!normalized)return;window.showToast?.('Mengambil logo dari website klien...');const result=await resolveLogo(normalized);const db=read();const clients=db.clients||[];const host=new URL(normalized).hostname.replace(/^www\./,'');const client=clients.find(c=>{const current=normalizeWebsite(c.website);if(current){try{if(new URL(current).hostname.replace(/^www\./,'')===host)return true;}catch{}}if(previousWebsite&&normalizeWebsite(c.website)===normalizeWebsite(previousWebsite))return true;return name&&String(c.name||c.legalName||'').trim().toLowerCase()===String(name).trim().toLowerCase();});if(!client)return;client.website=normalized;client.logoFetchStatus=result?'resolved':'not_found';client.logoFetchedAt=new Date().toISOString();if(result){client.logo=result.url;client.logoUrl=result.url;client.logoSourceUrl=result.url;client.logoSourceType=result.type;client.logoStorage='external';window.showToast?.('Logo klien berhasil diambil otomatis','success');}else window.showToast?.('Logo website tidak ditemukan; logo sebelumnya dipertahankan','error');write(db);paintLogos();}
function initials(name){return String(name||'').split(/\s+/).map(x=>x[0]).slice(0,2).join('').toUpperCase();}
function paintLogos(){const clients=read().clients||[];document.querySelectorAll('#clientRows tr').forEach(row=>{const name=row.querySelector('.pm-client-cell strong')?.textContent?.trim();const client=clients.find(c=>String(c.name||'').trim().toLowerCase()===String(name||'').toLowerCase());const holder=row.querySelector('.pm-client-logo');const src=client?.logoUrl||client?.logo;if(!holder||!src||holder.dataset.logoSrc===src)return;holder.dataset.logoSrc=src;holder.style.cssText+=';overflow:hidden;background:#fff;border:1px solid #e5e7eb;padding:3px';const img=document.createElement('img');img.src=src;img.alt=`Logo ${client.name}`;img.referrerPolicy='no-referrer';img.loading='lazy';img.style.cssText='display:block;width:100%;height:100%;object-fit:contain;border-radius:7px';img.onerror=()=>{holder.removeAttribute('data-logo-src');holder.textContent=initials(client.name);};holder.replaceChildren(img);});document.querySelectorAll('.phase0-client-logo img').forEach(img=>{const client=clients.find(c=>c.name===img.alt);const src=client?.logoUrl||client?.logo;if(src)img.src=src;});}
function install(){document.addEventListener('submit',event=>{const form=event.target;if(!(form instanceof HTMLFormElement)||!looksLikeClientForm(form))return;const websiteField=findWebsiteField(form);const website=websiteField?.value||'';if(!website.trim())return;const name=findNameField(form)?.value||'';const previousWebsite=websiteField?.dataset.originalValue||'';setTimeout(()=>updateSavedClient({website,name,previousWebsite}).catch(error=>{console.warn('client_logo_auto_failed',error);window.showToast?.('Data klien tersimpan, tetapi logo gagal diambil','error');}),350);},true);const observer=new MutationObserver(()=>{document.querySelectorAll('form').forEach(form=>{const field=findWebsiteField(form);if(field&&!field.dataset.originalValue)field.dataset.originalValue=field.value||'';});paintLogos();});observer.observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('proqtrack:db-updated',paintLogos);window.addEventListener('hashchange',()=>setTimeout(paintLogos,0));paintLogos();}
install();
export {};
